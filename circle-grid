function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(0);
  
  for (var x = 25; x < width; x += 25) {
    for (var y = 25; y < height; y += 25) {
    ellipse(x, y, 20);
  }
  }  
}
